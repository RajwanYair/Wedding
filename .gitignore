# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE (keep .vscode/ tracked for workspace config)
*.swp
*.swo
*~

# Node (zero-dependency project, but future-proof)
node_modules/
coverage/
dist/

# Lint caches (stored in node_modules/.cache — already ignored, but explicit)
.eslintcache
.stylelintcache

# Misc
*.log
*.bak
*.tmp
.extracted.*

# Legacy (deleted in v5.3.0 — still on disk for reference)
js/
sw.js
