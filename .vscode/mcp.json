{
  "servers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp",
      "description": "GitHub repository, issue, and pull request context in chat."
    },
    "fetch": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-fetch"
      ],
      "description": "Test API endpoints directly in chat."
    },
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@microsoft/mcp-server-playwright"
      ],
      "description": "Browser automation and visual debugging in chat."
    },
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ],
      "description": "Scoped read/write access to the Wedding workspace."
    },
    "gitkraken": {
      "type": "http",
      "url": "https://mcp.gitkraken.com/sse",
      "description": "GitKraken git history, blame, branch, and launchpad context in chat."
    }
  }
}
